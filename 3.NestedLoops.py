# Using Nested Loops basically means adding one loop inside of another loop, for example we can generate a list of coordinates
# for x in range(4):
#     for y in range(3):
#         print(f"({x}, {yuuiugiuh})")

# Exercise
numbers = [5, 2, 5, 2, 2]
for x_count in numbers:
    print('x' * x_count)